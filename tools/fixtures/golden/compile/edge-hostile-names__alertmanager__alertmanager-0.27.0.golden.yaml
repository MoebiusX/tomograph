# Alertmanager config compiled from ObservabilityPack v1.2
# Pack: edge-hostile-names · version 0.1.0
# Source of truth — DO NOT hand-edit. Re-emit from the pack.
# Generated by tools/lib/compile.mjs
global:
  resolve_timeout: 5m
  smtp_smarthost: smtp.example.internal:587
  smtp_from: alertmanager@edge-hostile-names.example.internal
route:
  receiver: "null"
  group_by:
    - alertname
    - severity
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 12h
  routes:
    - receiver: edge-hostile-names-sev1
      group_by:
        - slo
        - severity
        - alertname
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 1h
      matchers:
        - severity="SEV1"
        - service="edge-hostile-names"
    - receiver: edge-hostile-names-sev2
      group_by:
        - slo
        - severity
        - alertname
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h
      matchers:
        - severity="SEV2"
        - service="edge-hostile-names"
receivers:
  - name: "null"
  - name: edge-hostile-names-sev1
    msteams_configs:
      - webhook_url_file: /etc/alertmanager/secrets/msteams_oncall-alerts-escalations
        send_resolved: true
        title: "#oncall — \"alerts\" & escalations · {{ .CommonLabels.alertname }}"
  - name: edge-hostile-names-sev2
    email_configs:
      - to: team+alerts@example.com
        send_resolved: true
