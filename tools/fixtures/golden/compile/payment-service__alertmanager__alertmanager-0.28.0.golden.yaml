# Alertmanager config compiled from ObservabilityPack v1.2
# Pack: payment-service · version 1.5.0
# Source of truth — DO NOT hand-edit. Re-emit from the pack.
# Generated by tools/lib/compile.mjs
global:
  resolve_timeout: 5m
route:
  receiver: "null"
  group_by:
    - alertname
    - severity
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 12h
  routes:
    - receiver: payment-service-sev1
      group_by:
        - slo
        - severity
        - alertname
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 1h
      matchers:
        - severity="SEV1"
        - service="payment-service"
    - receiver: payment-service-sev2
      group_by:
        - slo
        - severity
        - alertname
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h
      matchers:
        - severity="SEV2"
        - service="payment-service"
    - receiver: payment-service-sev3
      group_by:
        - slo
        - severity
        - alertname
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h
      matchers:
        - severity="SEV3"
        - service="payment-service"
receivers:
  - name: "null"
  - name: payment-service-sev1
    msteamsv2_configs:
      - webhook_url_file: /etc/alertmanager/secrets/msteams_payments-oncall
        send_resolved: true
        title: "#payments-oncall · {{ .CommonLabels.alertname }}"
    pagerduty_configs:
      - service_key_file: /etc/alertmanager/secrets/pagerduty_payments
        details:
          channel: pagerduty://payments
    webhook_configs:
      - url_file: /etc/alertmanager/secrets/whatsapp_group-payments-oncall
        send_resolved: true
  - name: payment-service-sev2
    msteamsv2_configs:
      - webhook_url_file: /etc/alertmanager/secrets/msteams_payments-oncall
        send_resolved: true
        title: "#payments-oncall · {{ .CommonLabels.alertname }}"
    pagerduty_configs:
      - service_key_file: /etc/alertmanager/secrets/pagerduty_payments-low
        details:
          channel: pagerduty://payments-low
  - name: payment-service-sev3
    msteamsv2_configs:
      - webhook_url_file: /etc/alertmanager/secrets/msteams_payments-team
        send_resolved: true
        title: "#payments-team · {{ .CommonLabels.alertname }}"
inhibit_rules:
  -
    source_matchers:
      - maintenance="true"
    target_matchers:
      - service="payment-service"
    equal:
      - service
